# 重构状态与验收清单

> 基线：2026-09-03。范围：货物／货柜／制造商、玩家档案、装卸与制造／售货交易、生产线、贸易点、世界表面 UI、旧终端清理及对应文档。状态定义见[文档中心](README.md)。

## 已实现

| 系统 | 当前能力 | 正式文档 |
| --- | --- | --- |
| 货物 | 固定 24 个 CargoClass（分类贴花）；可扩展 CargoGood（独立 UI 图标＋悬浮名称组件，缺图直接问号）；未知类保留完整 ID；科技等级、标签、币种和运输要求 | [货物与分类](01货柜系统/01核心系统/05_货物类型.md) |
| 容器与制造商 | 容量、空重、耐久与兼容 CargoClass；制造价格来自制造台配方，制造商倍率、防护与缺陷参与解析 | [容器](01货柜系统/01核心系统/02_货柜定义系统.md)、[制造商](01货柜系统/01核心系统/08_制造商定义.md) |
| 资产与存档 | 统一 CargoContainerInstance；空柜省略 Load；卸空保留资产；无旧格式迁移 | [运行时与存档](01货柜系统/01核心系统/07_运行时与存档.md) |
| 交易 | 空柜制造购买；装载、补装、部分／全部卸货；装载机与卸载机执行服务端串行交易，库存、货币、Load、贸易点、经验与统计支持反向补偿；物理质量和贴花在提交后刷新 | [装载机](03功能方块/13_货柜装载机.md)、[卸载机](03功能方块/14_货物卸载机.md) |
| 物品售货机 | 严格商店定义、注册时稳定抽取、既有 output stock 标签匹配、服务器串行补偿购买、按实际 CargoGood `t` 记账、公共待领取区、部分领取和拆除掉落 | [商店系统](04商店系统/01_商店体系.md) |
| 世界表面 UI | 独立画布、`DISPLAY`／`OVERLAY`／`CONTROL` 表面、公共命中与服务端复核；提供八类封装仪表和四类交互控件；按钮／滑轨模型、命中与细高亮同源定位；仪表命中时沿实际绘制范围高亮并在准星旁显示用途；已接入大型货柜制造台等正式终端 | [世界表面 UI 系统](10世界表面UI系统/README.md) |
| 旧终端收口 | 旧装载／装卸终端、旧单方块货柜制造机和旧商店终端及其 GUI、菜单、网络、资源和逻辑已删除；旧贸易点终端只保留方块、物品和模型作为装饰 | [终端总览](03功能方块/01_买入终端与出售终端.md) |
| 生产线与贸易点 | 路径 ID、固定／随机产线、稳定输入绑定、五个有序启用槽位、槽位优先生产、日结、价值型活跃度和存档 | [经济运行](02贸易点系统/01_贸易点运行时与经济.md) |
| 加载 | 货物、制造商、容器、生产线池及贸易点关联数据校验后整体发布 | [维护流程](06开发与运维/01_数据包维护流程.md) |

内置内容基线为 20 个 CargoGood、52 个容器、8 个制造商；这是默认测试内容数量，不是扩展限制，也不代表平衡完成。

## 预留

| 项目 | 当前边界 |
| --- | --- |
| 货柜机械台 | 已注册占位方块；维修、改装、清理尚未开放 |
| 搭建台回收 | 当前只有空货柜制造购买；旧整柜收购不是新回收系统 |
| 货柜专用词条 | 尚未设计落地；现有运输词条只作用于 Load |
| 耐久玩法 | 已保存／显示耐久，不等于实现完整损坏、维修、报废循环 |
| 制造商声望 | 仅解析等级与阈值，无玩家积累、持久化和奖励闭环 |
| 贸易点 modules | 已实现按模块 ID 的定义池、注册时稳定抽取和存档；`item_shop` 与 `cargo_manufacturing_platform` 已分别接入物品售货机和货柜制造台；模块不反向生成库存或生产线 |
| 产线重抽与升级增线 | 现有产线和输入绑定固定；升级仅按规则改变容量与等级 |
| 生产随机倍率 | 设计未确定范围，目前固定 1.0，无额外配置项 |
| 内容平衡 | 工厂／市场配方网络与正式经济平衡未完成 |
| 更多世界表面终端 | 七个正式终端已接入；航运等独立界面尚未迁移，禁用态、冷却和运动结构支持仍预留 |

这些后续功能不阻止当前已实现契约进入正式文档。

## 待验收：已知一致性问题

| 编号 | 当前事实／风险 | 收口标准 |
| --- | --- | --- |
| R1 | 已实现：预览与世界实体均按 Load 解析；实体刷新从原始模板读取受管理槽位，支持 Sable 嵌入世界和持久化重试 | 自动化已收口；空柜→装货→卸空的世界观感纳入实机矩阵 |
| R2 | 补装容量来自容器快照，部分质量和货物参数来自当前定义；补装重建整批货物快照 | 明确重载后是否允许合批及参数来源，统一记录、预览、实体质量和结算币种 |
| R3 | 同名运输词条当前后加入者覆盖参数，临时设计要求按词条合并 | 确认覆盖或合并规则，并同步实现与测试，不把尚未实现的叠加写成现状 |
| R4 | 已实现：装卸共享服务端串行边界，全部权威变更注册反向补偿；同 Tick 重复动作抑制，故障注入、互斥和生命周期测试通过 | 自动化已收口；双客户端、高延迟和断线重试纳入实机矩阵 |
| R5 | 已实现：售货机以标签匹配结果中的每种具体 CargoGood 实际消耗 `t` 为唯一库存与活跃度记账依据，整笔付款只累计一次，并纳入串行补偿事务 | 自动化代码边界已收口；双客户端最后库存竞争、断线和重复输入纳入实机矩阵 |

代码定位（主仓库，相对 `src/main/java/cn/or15/cargoverse/`）：R1 为 `cargo/CargoPhysicalAppearanceService`、`ship/CargoContainerSpawner`、`menu/CargoStructurePreviewSnapshot`；R2/R3 为 `trade/CargoLoadingService`；R4 为装载／卸载交易服务、`CargoTradeTransaction` 与 `CargoTradeSerialExecutor`；R5 为 `trade/ItemVendingMachineTransaction`、`shop/ItemShopOutputMatcher` 和 `trade/TradePointTransactionService`。

## 验证基线与待执行清单

前次评估执行 `gradlew.bat build --no-daemon test --rerun`：构建成功，302 项测试通过、0 失败、0 跳过。此为自动化基线，不是本轮文档修改后重新启动 Minecraft 的记录。

2026-08-28 CargoGood UI 图标框架及职责拆分追加验证：`gradlew.bat build --console=plain` 成功，319 项测试通过、0 失败、0 错误、0 跳过。覆盖可选 icon 校验、名称／图标原子替换、网络包往返、缺图直接问号、缓存失效、默认 PNG 资源、组件统一调用与贴花解耦、变换坐标命中及地图详情悬停保持；不代表已完成下列实机项目。

2026-08-28 贸易点经济仪表板追加验证：`gradlew.bat build --console=plain` 成功，327 项测试通过、0 失败、0 跳过。新增覆盖指标格式、倒计时、固定布局边界、绿灰分段进度计算和素材尺寸；不表示已经完成游戏内视觉验收。

2026-08-29 世界表面 UI 第一版底层追加验证：`gradlew.bat build --console=plain` 成功，336 项测试通过、0 失败、0 错误、0 跳过。新增覆盖定义／区域约束、六向坐标变换、公共命中、画布不一致拒绝、服务端安全入口和 Screen／Menu 独立性；尚未接入终端，因此不代表世界内显示与交互已经实机验收。

2026-08-29 世界表面 UI 分辨率测试屏追加验证：`gradlew.bat build --console=plain` 成功，340 项测试通过、0 失败、0 错误、0 跳过。新增覆盖五种画布定义、测试方块资源、创造模式入口及六个方块表面朝向；已修正屏幕坐标系手性错误导致测试屏放置后首帧崩溃的问题，实机视觉选择仍需按本章步骤执行。

2026-08-30 封装仪表阶段整理追加验证：`gradlew.bat verifyCargoVerse --console=plain` 成功，421 项测试通过、0 失败、0 错误、0 跳过。公共纹理寻址、百分比换算与 GUI 着色复位已收口，生产线终端和贸易点信息柜继续通过终端组合层调用仪表；该结果不替代低光环境、远近距离与动画观感的实机验收。

2026-08-30 仓库信息柜追加验证：`gradlew.bat verifyCargoVerse --console=plain` 成功，428 项测试通过、0 失败、0 错误、0 跳过。新增覆盖 3×2 多方块资源、库存主屏与侧栏页码覆盖层、四个 Input／Output 翻页按钮面、独立四项分页、库存占比归一化、无标题／底部页码仪表布局、翻转牌文本截断和管理员 UUID 绑定链路；该结果不替代模型朝向、显示面贴合、按钮命中、低光环境及大量货物手动翻页的实机验收。

2026-08-30 交互按钮组件整理：移除通用无 GUI 数据输入类型，生产线终端与仓库信息柜统一改由 `WorldUiButtonSet` 声明实体按钮、触摸区域、服务端动作和动画元数据。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，434 项测试通过、0 失败、0 错误、0 跳过；该结果不替代多人、各朝向位移、低帧率动画观感和运动结构的实机验收。

2026-09-01 普通按钮组件：新增 `WorldUiButtonComponent`、组件目录与统一客户端 renderer，普通按钮封装 3×2 MCpx 模型、命中锚点、可信服务端动作安装、0.04 秒按下、0.08 秒弹起和 1 MCpx 行程，并接入仪表测试方块顶部。`gradlew.bat verifyCargoVerse --rerun-tasks` 成功，442 项测试通过、0 失败、0 错误、0 跳过；该结果不替代按钮安装朝向、实体模型与命中面贴合、多人同步和实际按压观感的实机验收。

2026-09-01 按钮组件单链路收尾：新增具名 `WorldUiButtonInstance`，将生产线终端、仓库信息柜和仪表测试方块全部迁移为组件实例；生产线按钮从运行时主体模型拆分，仓库旧专用按钮模型迁入统一组件目录；删除终端 renderer 内模型数组、ID 数组、直接按压动画和专用客户端注册数组，并将底层 `Builder.button(...)` 限制为按钮包内部可见。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，444 项测试通过、0 失败、0 错误、0 跳过；该结果不替代各朝向按钮位移方向、模型与命中面贴合、物品形态静态模型和多人同步的实机验收。

2026-09-01 保持型钮子开关组件：接入预拆分的底座、拨杆和贴图，新增 `WorldUiToggleComponent`／具名实例、统一模型注册、服务端布尔状态持久化与客户端枢点旋转 renderer；关闭状态为 X `-22.5°`，开启状态为 X `+22.5°`，围绕 `(8,1,9)` MCpx 枢点以 0.15 秒平滑切换，并接入仪表测试方块。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，448 项测试通过、0 失败、0 错误、0 跳过；该结果不替代各方块朝向、多人同时切换、低帧率插值、拆除重放和实际模型枢点观感的实机验收。

2026-09-01 四颜色带文字大按钮：接入 4×3×1 MCpx 大按钮模型及灰、绿、黄、红四张原始贴图，新增类型安全颜色枚举、具名标签实例和统一 renderer；标签限制为 16 个 Unicode 码点，在按钮顶面 3.5×2 MCpx 区域内自动缩放居中，使用普通场景光照，并与按钮共同执行 0.04 秒按下、0.08 秒弹起和 1 MCpx 行程。四种变体均已加入仪表测试方块。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，450 项测试通过、0 失败、0 错误、0 跳过；该结果不替代长翻译文本、各字体、各朝向、低光环境和按钮顶面文字方向的实机验收。

2026-09-01 有界数值滑轨组件：接入 `track`／`slider` 预拆分模型和原始贴图，新增可配置轨道长度、最小值、最大值、步进、数值标签、动态滑块命中面与客户端按住右键拖动控制。开始拖动必须由服务端重新射线命中滑块并签发会话；拖动采样持续复核令牌、维度、距离、区块、控制器、结构和滑轨表面，松键结束，断线／换维度／40 tick 无心跳自动清理。仪表测试方块已加入 16 MCpx、0–100、步进 1 的样例。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，456 项测试通过、0 失败、0 错误、0 跳过；该结果不替代锁定鼠标模式下横向采样、各朝向模型与命中面贴合、快速拖动、多人并行和高延迟环境的实机验收。

2026-09-01 滑轨输入微调：默认完整量程所需横向鼠标移动由 120 px 调整为约 480 px；会话激活期间通过可取消的交互键事件独占右键使用动作，阻止持续按住造成的重复开方块、物品使用、其他按钮误触和挥手，同时保留物理松键检测。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 再次成功，456 项测试通过、0 失败、0 错误、0 跳过；仍需实机确认不同鼠标 DPI 和游戏灵敏度下的最终手感。

2026-09-01 通用小按钮四向：新增局部 `UP`／`RIGHT`／`DOWN`／`LEFT` 方向参数，复用同一向上箭头模型与贴图；renderer 围绕 `(8,0,8)` MCpx 旋转整个按钮，左右方向同步将命中画布和物理尺寸由 3×2 交换为 2×3，并围绕按钮中心重新定位。既有终端专用按钮保持默认方向，仪表测试方块改为显式传入 `UP`。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，457 项测试通过、0 失败、0 错误、0 跳过；仍需实机确认四向箭头视觉、命中边界和终端整体朝向组合。

2026-09-01 交互控件阶段整理：新增 `WorldUiDirectionalButtonComponent`，使通用箭头按钮在编译期强制传入方向，固定朝向的终端专用按钮继续使用基础组件；普通按钮 renderer 删除可绕过具名实例的模型级公开重载。按钮与滑轨模型由 `WorldUiControlComponents` 单一目录入口注册并自动去重。仪表测试定义从客户端引导迁入共同端 `WorldUiBootstrap`，保证独立服务端也注册命中与动作；滑轨客户端状态改为不可变会话记录，服务端世界验证集中到控制器解析步骤。相关接入示例与章节入口已统一为“交互控件”。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain` 成功，458 项测试通过、0 失败、0 错误、0 跳过；该结果不替代独立服务端、多人滑轨会话和四向按钮组合的实机验收。

2026-09-01 大型装卸设备与旧终端收口：`container_loader` 已接入货柜／货物选择、1～100 数量滑轨、红色 `LOAD`、真实扣款扣库存和 Load／物理质量更新；`cargo_unloader` 已接入选柜、数量滑轨、绿色 `UNLOAD`、收购预览、真实入库结算和 Load／物理质量更新。旧 `cargo_loading_terminal`、`cargo_handling_terminal` 及其 Screen／Menu／网络／资源被架构测试锁定为不存在；`trade_point_terminal` 只保留装饰方块和模型。按钮与滑轨新增组件 `ModelFrame`／`Placement`，由同一 `SurfaceAnchor` 驱动模型、命中与细高亮。`gradlew.bat test --rerun-tasks` 成功，475 项测试通过、0 失败，共 159 个测试套件；该结果不替代下列实机项目。

2026-09-01 五项高优先级顺序收口：装卸交易纳入服务器级串行事务，货币、贸易点库存、货柜、许可证和统计均注册逆序补偿；世界实体贴花改为模板受管理槽位刷新，支持普通世界、Sable 嵌入世界和持久化重试；完整 Load 生命周期、同 Tick 重复动作、多线程竞争及五终端×四水平朝向表面矩阵加入自动化。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，484 项测试通过、0 失败、0 错误、0 跳过，共 164 个测试套件；`gradlew.bat runGameTestServer --console=plain --no-daemon` 的 18 项必需 GameTest 全部通过，三维度和 Sable 子世界保存后正常关闭。客户端视觉、双客户端、高延迟和运动结构仍按下列实机项目验收，不因本条自动关闭。

2026-09-02 动态货柜质量收口：装载、部分卸载、卸空与重新装载提交后，统一物理派生刷新从权威货柜资产重新计算总质量，更新实际结构内质量占位方块并同步贴花；Sable 适配器改用嵌入世界局部零点，避免 plot 中心二次偏移，随后重建质量／惯量追踪、发布统计并唤醒刚体。刷新暂不可用时以 `PendingPhysicalRefreshes` 持久化重试，并兼容读取早期 `PendingAppearanceRefreshes`。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，484 项测试通过、0 失败、0 错误、0 跳过，共 164 个测试套件；`gradlew.bat runGameTestServer --no-daemon` 的 19 项必需 GameTest 全部通过，其中新增用例实测占位方块与 Sable 刚体追踪质量同步。实机操控手感与存档重进仍保留下列验收项。

2026-09-02 贸易点功能模块接入：`modules` 从不透明数组升级为模块 ID 对象，注册时按贸易点实例稳定抽取并持久化定义；同定义重注册保留选择、同步解锁等级、补充新增模块并移除已删模块。`item_shop` 已接入商店终端与繁荣等级锁定，默认商店统一位于 `trade_points/trade_points_subfunctions/shop_definitions/resource/general/default_shop.json`，定义 ID 为 `cargoverse:resource/general/default_shop`；制造台模块先保留通用抽取与存档契约。旧 DataVersion 8 注册记录无模块字段仍可载入。

2026-09-02 贸易点结构绑定收口：`trade_point_structure_bindings` 强制按 `factory`、`market`、`resource` 分类，旧 `structures_pool`／`trade_point_id` 格式被严格的 `schema_version`、`structure`、`trade_point_definitions` 替代。绑定与贸易点定义纳入同一原子数据重载，缺字段、未知字段、非法或空候选、重复结构、跨类别和悬空引用均拒绝加载。`verifyCargoVerse` 成功，495 项测试通过、0 失败、0 错误、0 跳过，共 166 个测试套件；19 项服务器 GameTest 在一次既有 Sable 时序重试后全部通过。

2026-09-02 手工贸易点定义绑定收口：贸易点信息柜管理员 GUI 增加唯一的贸易点定义 ID 输入，UUID 保持自动生成且只读；服务端复核管理员模式、距离、控制器和已加载定义后更新“UUID → 定义”注册。结构生成桥不再向商店终端旁路注入定义，所有其他终端只保存 UUID；改绑不同定义会重建该 UUID 的运行时经济状态，避免复用不兼容的产线与库存键。网络协议提升至 20。`verifyCargoVerse --rerun-tasks` 成功，497 项测试通过、0 失败、0 错误、0 跳过，共 166 个测试套件；19 项服务器 GameTest 全部通过。

2026-09-02 贸易点绑定阶段清理：装载机、卸载机、货柜生成终端、出售终端、商店、生产线终端和仓库信息柜统一实现 `TradePointInstanceBindingTarget`，结构生成桥由七种具体方块实体分支收敛为“注册锚点／UUID 绑定目标”两类契约。注册锚点方法与普通终端方法分名，避免定义 ID 误传入普通终端；定义解析器删除“把终端填写值直接当定义 ID”的遗留回退，只从注册记录解析。`verifyCargoVerse --rerun-tasks` 成功，497 项测试通过、0 失败、0 错误、0 跳过，共 166 个测试套件；19 项服务器 GameTest 全部通过。

2026-09-02 终端未绑定空状态收口：手动放置的信息柜定义字段与所有功能终端 UUID 初始为空，空值不再回退到默认贸易点；未绑定时不创建经济状态，也不扫描或同步业务展示数据，但终端本体、仪表外壳、按钮和滑轨仍完整绘制，主状态仪表显示“未绑定”、其余栏位为空白或零位，服务端业务交互直接拒绝。自然生成结构仍通过严格结构绑定显式选择定义，管理员也可显式选择内置默认定义。`gradlew.bat test` 成功，518 项测试通过、0 失败、0 错误、0 跳过，共 173 个测试套件；该结果不包含本轮暂时跳过的管理员手动定义绑定故障修复。

2026-09-02 未绑定终端展示语义修正：五类世界表面终端的绑定状态进入显式 `ViewState`，渲染器不再因未绑定而跳过整套世界 UI；各仪表组合保留外壳并以“未绑定”或空白／零位显示，物理按钮与滑块保持可见，服务端业务动作仍拒绝。新增架构回归约束，`verifyCargoVerse --rerun-tasks` 成功，528 项测试通过、0 失败、0 错误、0 跳过，共 178 个测试套件。

2026-09-02 货柜制造价格权威收口：52 份内置货柜定义删除 `base_value`，制造商删除失效的 `value_multiplier`，旧字段在数据加载时显式拒绝；设备解析和运行时快照不再保存货柜基础价值。货柜搭建台按贸易点注册记录中抽中的 `cargo_manufacturing_platform` 定义列出具体配方，报价、扣款币种与金额统一读取 recipe `currency_cost`，繁荣度和许可证限制也来自同一配方；复合销售槽位键允许同一货柜存在多条配方。网络协议提升至 21。`verifyCargoVerse --rerun-tasks` 成功，532 项测试通过、0 失败、0 错误、0 跳过，共 179 个测试套件。

2026-09-02 货柜制造链路阶段整理：贸易点模块、定义、繁荣度和 `input_stock` 收敛为一次解析的制造目录快照；持久化库存与运行时资源 ID 的转换进入独立边界，材料匹配保持纯计算，仪表快照由显示投影服务生成。购买入口缩减为串行调度门面，货币、库存、货柜生成与贸易统计集中到服务端补偿事务；旧单方块制造入口和管理员菜单改为一次读取目录后派生全部展示数据。新增库存快照规范化和架构边界回归测试。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，550 项测试通过、0 失败、0 错误、0 跳过，共 184 个测试套件；`gradlew.bat runGameTestServer --console=plain --no-daemon` 的 19 项必需 GameTest 全部通过并正常保存关闭。制造台客户端仪表、按钮观感及多客户端竞争仍按实机项目继续验收。

2026-09-03 旧单方块货柜搭建台完整移除：删除 `cargoverse:cargo_assembly_terminal` 的方块、物品、方块实体、菜单、Screen、客户端状态／布局、三个网络包、旧购买门面、注册、语言和模型资源；大型 `cargoverse:cargo_manufacturing_platform` 改为独立方块实体，并通过 `CargoManufacturingPurchaseService` 进入既有服务端补偿事务。货柜出售或删除不再回写已废弃的“最后生成货柜”终端状态。仍保留的货柜机械台改用自身命名的模型和贴图，不再依赖旧资源 ID。网络协议提升至 23。`verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，543 项测试通过、0 失败、0 错误、0 跳过，共 181 个测试套件；19 项必需 GameTest 全部通过并正常保存关闭。旧存档中的已删除方块 ID 不提供兼容别名，实机测试前应备份世界。

2026-09-03 封装仪表瞄准反馈：八类仪表实例统一持有 `InstrumentInfo`，普通平面仪表直接以等比适配后的实际绘制矩形完成准星命中和细边框，全息投影以实体底座世界范围完成命中；所有候选统一由 HUD 选择最近项并在准星旁显示具体用途，终端组合层不再维护独立悬停坐标。五类既有世界表面终端、大型货柜制造台和仪表测试方块均已标注具体用途，空值／未绑定状态仍可识别仪表。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，549 项测试通过、0 失败、0 错误、0 跳过，共 182 个测试套件；边框贴合、相邻目标选择和不同 FOV 下 HUD 遮挡仍需实机验收。

2026-09-03 货柜制造台仪表提示首轮映射：按模型栏位固定货柜名称、制造配方页码、可运输货物类型、货柜说明、可满足运输要求、占用调度点、最大载货量、空货柜质量、最大耐久、三行制造材料／库存状态、制造费用和货柜模型预览等具体用途；制造台组合层只配置语义，命中矩形、高亮与准星 HUD 继续由仪表封装统一处理。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，550 项测试通过、0 失败、0 错误、0 跳过，共 182 个测试套件；仍需在四水平朝向、斜面边缘和相邻栏位交界处实机确认高亮归属。

2026-09-03 物品售货机购买与生命周期收口：BUY 请求进入服务器级串行补偿事务，在锁内重新确认 UUID 绑定、模块定义、繁荣度、当前商品、购买数量、货币和贸易点 `output_stock`；标签成本只从贸易点已存在的 CargoGood 中按稳定顺序匹配并扣除，交易统计使用实际消耗的小数 `t` 数和各货物价值记账。货币、库存、公共待领取区和统计形成逆序补偿边界，成功提交后才刷新仪表；玩家从出货副方块领取，背包空间不足时只取可容纳部分，其余继续暂存，任意玩家均可领取。暂存区为空时不渲染货箱，终端拆除时只由控制器清空并掉落暂存物。商店结果物品 ID 在完整数据重载发布前统一校验，不再由普通 JSON 解析提前初始化游戏注册表。`gradlew.bat verifyCargoVerse --rerun-tasks --console=plain --no-daemon` 成功，545 项测试通过、0 失败、0 错误、0 跳过，共 180 个测试套件；`gradlew.bat runGameTestServer --console=plain --no-daemon` 的 19 项必需 GameTest 全部通过并正常保存关闭。本阶段没有新增自动测试；双客户端最后库存竞争、跨玩家领取、满背包／部分领取及拆除掉落仍保留为实机验收项。

- [ ] 新测试世界：制造空柜→装货→运输→部分卸货→卸空→重新装载。
- [ ] 所有权、扫描范围、不兼容货物、非需求货物、满库存、超载、余额不足。
- [ ] 空输入显示总价 0；超过库存裁剪；完整度混合与剩余购入成本正确。
- [ ] 实体质量、GUI 质量、贴花、词条状态在装卸和存档重进后保持一致（R1/R2）。
- [ ] 数据包修改货物质量／币种／词条／兼容性后补装；删除并恢复货物定义（R2/R3）。
- [ ] 贸易点固定／随机绑定、输入扣料、分数产出、长时间离线、时间回退。
- [ ] 多终端／多玩家共享库存、网络重复请求及交易异常恢复（R4）。
- [x] 物品售货机按 `output_costs × 购买数量` 扣除每种实际 CargoGood，并以同一小数 `t` 数更新吞吐与活跃度（R5）；真实双客户端竞争仍在实机项中。
- [ ] 物品售货机：最后库存双人竞争、跨玩家领取、满背包／部分领取、暂存物重进、拆除只掉落一次。
- [ ] 大型货柜制造台：管理员 GUI 输入校验，以及世界表面配方切换、材料／货币显示、模型投影与制造按钮命中。
- [ ] 贸易点信息柜：0%／73%／100% 活跃度格数、倒计时跨日同步、有效贸易额累积／日结归零；管理员绑定有效／无效定义、UUID 只读、手工多终端共享；装饰性的旧贸易点终端不应再打开任何 GUI。
- [ ] CargoGood 专属图标联机同步；`/reload` 替换或移除图标；资源包热重载补图；专属缺图直接问号，不读取 CargoClass 贴花。贴花及存档不应受 UI 图标变化影响。
- [ ] 所有终端货物图标悬浮名称一致；淡显、滚动、裁剪和地图缩放下命中正确；地图详情框允许鼠标移入货物图标，提示不被面板遮挡。
- [ ] 世界表面终端：生产线终端、贸易点信息柜、仓库信息柜、货柜装载机、货物卸载机、货柜制造台与物品售货机的四朝向／多方块绘制、斜面、光照、按钮／滑块细高亮、多人、结构瞬时破坏、控制器卸载、revision、专用服务端及运动结构坐标投影。
- [ ] 装载机：货柜／货物四向按钮、透明画布、两条增减仪表、红色 `LOAD`、1～100 滑轨以及实际扣款、扣库存、更新 Load／质量。
- [ ] 卸载机：不可出售时只清空上方收购区并保留下方货柜信息、数量裁剪、完整度结算、绿色 `UNLOAD`、实际入库、发币、更新 Load／质量。
- [x] 五档分辨率测试屏已完成实机对比：普通终端采用 8 px／MCpx（单方块 128×128）；近距离精细屏采用 16 px／MCpx（256×256）；大字体显示采用 4 px／MCpx（64×64）；多方块大型信息屏采用 1 px／MCpx（16×16）。32×32 保留为过渡测试档。

使用新世界或已备份的测试世界；本次文档操作不删除、转换或修复玩家存档。

## 文档整编边界

已实现契约进入正式入口；未完成功能单列预留；不一致与实机检查列待验收。临时文档和历史版本保留以便追溯，不能因为本文建立就宣告整轮重构验收通过。
